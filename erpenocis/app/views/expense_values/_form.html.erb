<%= form_with(model: expense_value) do |form| %>
  <% if expense_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense_value.errors.count, "error") %> prohibited this expense_value from being saved:</h2>

      <ul>
        <% expense_value.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :expense_id %>
    <%= form.select :expense_id, Expense.all.map {|c| [c.name, c.id]} %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :due_date %>
    <%= form.date_select :due_date %>
  </div>

  <div class="field">
    <%= form.label :vat_taxes %>
    <%= form.check_box :vat_taxes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
